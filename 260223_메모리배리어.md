### 컴파일러 최적화

내가 짠 코드를 컴파일러가 지 입맛대로 바꿔가지고 다른 결과물을 내뿜음 → 멀티 스레드 환경에서 독이 됨

## 메모리 배리어

멀티 스레드 환경에서 우리가 예상한 로직이 꼬이지 않기 위하여 고안해낸 방법

MemoryBarrier() 호출을 하게되면 호출 전 메모리 액세스와 호출 후 메모리 액세스는 서로 접근 할 수 없게 됨

예시)

이를 닦기 전에는 과자를 마음껏 먹을 수 있지만 이를 닦고난 후에는 찝찝해서 안먹는 상태(?)

과자는 이를 닦고난 후 상황에 범접할 수 없다(?)

### 사용?

사용할 순 있지만 주로 커널 개발자가 사용하기 때문에 개념정도만 알아도 될 듯하다?

그래서 보통 하드웨어적인 지원을 받아 critical section 코드를 작성함
